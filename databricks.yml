# $schema: ./bundle_config_schema.json
bundle:
  name: hj-krc-dabs
  engine: direct

include:
  - ./configuration/*/*.yml
  - ./configuration/*.yml

targets:
  dev:
    default: true
    mode: development