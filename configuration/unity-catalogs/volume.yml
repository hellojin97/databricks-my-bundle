# $schema: ../../bundle_config_schema.json
resources:
  volumes:
    main-volume:
      name: hj-main-volume
      comment: "Main volume"
      catalog_name: ${resources.catalogs.hj-krc-main.name}
      schema_name: ${resources.schemas.hj-krc-main.name}
