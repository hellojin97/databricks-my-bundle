# $schema: ../../bundle_config_schema.json
resources:
  catalogs:
    main-catalog:
      name: hj-krc-main
      comment: "Catalog for DA's HYEONJIN KIM (MANAGED BY DAB DIRECT)"
      grants:
        - principal: "Databricks_JIN_ADMIN"
          privileges:
            - ALL_PRIVILEGES
            - MANAGE